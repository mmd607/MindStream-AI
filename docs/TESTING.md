# Testing

Run `pytest backend/tests -q` from the repository root after installing `backend/requirements.txt`. The test suite covers validation, mock provider structure, topological ordering/cycle rejection, health, project CRUD, generation, result retrieval, exports, and missing-resource errors.

