"use client";
import { useEffect,useState } from "react"; import { useParams } from "next/navigation"; import { api } from "@/lib/api"; import { Empty,ErrorState,Loading } from "@/components/ui"; import type { Document } from "@/types";
export default function DocumentationPage(){const{id}=useParams<{id:string}>();const[data,setData]=useState<Document[]|null>(null);const[error,setError]=useState("");useEffect(()=>{api.documentation(id).then(setData).catch(e=>setError(e.message));},[id]);if(error)return <div className="py-10"><ErrorState message={error}/></div>;if(!data)return <div className="py-10"><Loading/></div>;return <div className="py-10"><p className="eyebrow">Blueprint / handoff</p><h1 className="mt-2 text-4xl font-bold">Documentation</h1><p className="muted mt-3">A human-readable project blueprint for the team to review and implement.</p>{data.length===0?<div className="mt-8"><Empty/></div>:<article className="card mt-8 overflow-hidden"><pre className="overflow-x-auto whitespace-pre-wrap p-7 text-sm leading-7 text-slate-700">{data[0].content}</pre></article>}</div>}

