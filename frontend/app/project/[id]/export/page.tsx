"use client";
import { useParams } from "next/navigation"; import { Card } from "@/components/ui";
export default function ExportPage(){const{id}=useParams<{id:string}>();const base=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000/api/v1";return <div className="py-10"><p className="eyebrow">Blueprint / handoff</p><h1 className="mt-2 text-4xl font-bold">Export</h1><p className="muted mt-3">Download the validated project plan for review, sharing, or a portfolio artifact.</p><div className="mt-8 grid gap-6 sm:grid-cols-2"><Card title="Markdown"><p className="muted text-sm leading-6">Readable overview with requirements, architecture, database, tasks, roles, and assumptions.</p><a className="focus-ring mt-6 inline-block rounded-xl bg-indigo-600 px-4 py-3 text-sm font-semibold text-white" href={`${base}/projects/${id}/export?format=markdown`}>Download .md</a></Card><Card title="JSON"><p className="muted text-sm leading-6">Structured data for future integrations and programmatic processing.</p><a className="focus-ring mt-6 inline-block rounded-xl border border-slate-300 px-4 py-3 text-sm font-semibold" href={`${base}/projects/${id}/export?format=json`}>Download .json</a></Card></div></div>}

