import { Shell } from "@/components/ui";
import Link from "next/link";

const links = [{ href: "", label: "Overview" }, { href: "/requirements", label: "Requirements" }, { href: "/architecture", label: "Architecture" }, { href: "/database", label: "Database" }, { href: "/apis", label: "APIs" }, { href: "/tasks", label: "Tasks" }, { href: "/team", label: "Team" }, { href: "/reports", label: "Reports" }, { href: "/risks", label: "Risks" }, { href: "/activity", label: "Activity" }, { href: "/documentation", label: "Docs" }, { href: "/export", label: "Export" }];

export default async function ProjectLayout({ children, params }: { children: React.ReactNode; params: Promise<{ id: string }> }) { const { id } = await params; return <Shell><div className="mx-auto max-w-7xl px-6 py-8"><Link href="/" className="muted text-sm">← All projects</Link><nav aria-label="Project sections" className="mt-8 flex gap-2 overflow-x-auto border-b border-slate-200 pb-2">{links.map(link => <Link key={link.label} href={`/project/${id}${link.href}`} className="focus-ring whitespace-nowrap rounded-lg px-3 py-2 text-sm font-semibold text-slate-600 hover:bg-indigo-50 hover:text-indigo-700">{link.label}</Link>)}</nav>{children}</div></Shell>; }
